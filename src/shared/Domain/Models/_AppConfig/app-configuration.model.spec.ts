import { AppConfiguration } from './app-configuration.model';

describe('AppConfiguration', () => {
  it('should create an instance', () => {
    expect(new AppConfiguration()).toBeTruthy();
  });
});
